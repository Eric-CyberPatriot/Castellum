sudo pdbedit -L -v command can be used to check which accounts have been disabled using the smbpasswd utility.
A disabled account will usually have [D ] or [DISABLED ] or a flag that clearly indicates "disabled".
